# Monorepo: install with pnpm from repo root (npm does not support workspace:*)
[phases.setup]
nixPkgs = ["nodejs_20", "pnpm"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm --filter @loyalty/api run build"]

[start]
cmd = "node apps/api/start.js"
