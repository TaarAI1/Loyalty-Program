[build]
builder = "nixpacks"

[deploy]
startCommand = "node scripts/migrate-deploy.js && node dist/main"
healthcheckPath = "/api/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
